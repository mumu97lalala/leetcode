var removeDuplicates = function (nums) {
    let length = nums.length;
    for (let i = 0; i < length; i++) {
        if (nums[i] != nums[i + 1]) {
            nums.push(nums[i]);
        }
    }
    nums.splice(0, length);
    return nums.length;
};
